# Desenvolva um programa que leia as duas notas de um aluno, calcule e mostre a sua média.
número1 = float(input('Digite a primeira nota: '))
número2 = float(input('Digite a segunda nota: '))
print('A média entre {:.1f} e {:.1f} é {:.1f}.'.format(número1, número2, (número1 + número2) / 2))
      